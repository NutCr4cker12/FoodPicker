const transactionState = {
    categories: [],
    categoryTypes: []
}

export default (state = transactionState, action) => {
    switch (action.type) {
        
        default:
            return state;
    }
}